version: '3'

services:
  app:
    container_name: app
    build: .
    ports:
      - 8501:8501

  reverse:
    container_name: reverse
    hostname: reverse
    image: nginx:latest
    ports:
      - 80:80
      - 443:443
    volumes:
      - ~/nginx:/etc/nginx
